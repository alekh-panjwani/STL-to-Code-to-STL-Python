# STL-to-Code-to-STL-Python
RUN THE REQUIREMENTS FILE BEFORE EXECUTING CODES FOR STL FILES 
pip3 install -r requirements.txt

Next Run individual .py files to get output .stl for each part.

Finally run assembly.py
